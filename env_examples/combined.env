# Combined environment generated by combine_envs.py
# Sources: dev=env_examples/dev.env, staging=env_examples/staging.env, prod=env_examples/prod.env
# Common values appear once; differing values include notes per environment.

# Common values

# API_URL varies: dev=http://localhost:3000, staging=https://staging.example.com, prod=https://app.example.com
API_URL=http://localhost:3000

# DB_HOST varies: dev=localhost, staging=staging-db.internal, prod=prod-db.internal
DB_HOST=localhost
DB_PORT=5432

# FEATURE_FLAG_NEW_UI varies: dev=true, staging=true, prod=false
FEATURE_FLAG_NEW_UI=true

# LOG_LEVEL varies: dev=debug, staging=info, prod=warning
LOG_LEVEL=debug
